{
  "name": "v2x-tools-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i styles.css -o styles.css --minify",
    "watch": "tailwindcss -i styles.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
